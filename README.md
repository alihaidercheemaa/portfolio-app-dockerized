# React Portfolio

A small Vite + React portfolio scaffold with Framer Motion animations and React Router.

How to run:

```powershell
cd d:\Docker\frontend-project
npm install
npm run dev
```

Build:

```powershell
npm run build
npm run preview
```

Feel free to customize the pages in `src/pages/` and styles in `src/styles.css`.
